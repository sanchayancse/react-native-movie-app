import * as React from 'react';
import {Text, View, StyleSheet} from 'react-native';

interface SeatBookingScrrenProps {}

const SeatBookingScrren = (props: SeatBookingScrrenProps) => {
  return (
    <View style={styles.container}>
      <Text>SeatBookingScrren</Text>
    </View>
  );
};

export default SeatBookingScrren;

const styles = StyleSheet.create({
  container: {},
});
